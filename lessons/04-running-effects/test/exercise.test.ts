import { describe, it } from "@effect/vitest"

describe("Lesson 04 exercise: runtime boundaries", () => {
  it.todo("runs the support console boundary and returns a plain refund preview")
  it.todo("inspects a console boundary failure as Exit data")
  it.todo("runs the support API boundary and returns a Promise result")
  it.todo("inspects an API boundary failure as Promise<Exit>")
  it.todo("proves prepareRefundPreview is lazy until a boundary runs it")
})
